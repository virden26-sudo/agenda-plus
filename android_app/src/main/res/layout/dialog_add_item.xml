<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/sync_from_text_instructions"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/etItemDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/sync_from_text_instructions"
        android:inputType="textMultiLine"
        android:minLines="3"
        android:importantForAutofill="no" />

    <Button
        android:id="@+id/btnSyncFromStorage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/sync_from_local_storage" />

    <Button
        android:id="@+id/btnSyncFromPortal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/sync_from_portal" />

</LinearLayout>
